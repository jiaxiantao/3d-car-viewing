# docs/

GitHub Pages is deployed via **GitHub Actions** (`actions/upload-pages-artifact` + `actions/deploy-pages`).

Static export output lives in `out/` locally and is **not** committed here.

See [documentation/GITHUB_PAGES_SETUP.md](../documentation/GITHUB_PAGES_SETUP.md).
